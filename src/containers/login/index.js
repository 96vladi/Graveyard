import React from 'react';
import { Text } from 'react-native';
import { Layout } from '../../components/general';

export const Login = () => {
  return(
    <Layout>
      <Text>Hola</Text>
    </Layout>
  );
};
